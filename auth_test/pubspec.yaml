name: tekartik_firebase_auth_test
description: Firebase auth test
version: 0.9.0

publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  meta: ">=1.15.0"
  path: ">=1.9.0"
  uuid: '>=1.0.0'
  tekartik_firebase:
    git:
      url: https://github.com/tekartik/firebase.dart
      ref: dart3a
      path: firebase
  version: '>=0.4.0+0'
  tekartik_firebase_auth:
    git:
      url: https://github.com/tekartik/firebase_auth.dart
      path: auth
      ref: dart3a
    version: '>=0.9.0'
  test: ">=1.24.0"

dev_dependencies:
  process_run: ">=1.2.1+1"
  dev_build: ">=1.1.2"

resolution: workspace
